'use strict';

/* Filters */

/*var filteredData;

filteredData = $filter('query')(bestseller, function(data) {
  if ($scope.query) {
    return bestseller.book_details[0].title.toString().indexOf($scope.query) > -1;
  } else {
    return true;
  }
});*/

/* $scope.query = function (bestseller){
    if (bestseller.book_details[0].title.indexOf($scope.query)!=-1 || bestseller.book_details[0].author.indexOf($scope.query)!=-1) {
            return true;
        }
        return false;
  };*/